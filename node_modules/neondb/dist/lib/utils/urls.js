//#region src/lib/utils/urls.ts
const HOST = "https://neon.new";
const LAUNCHPAD_URLS = {
	GET_DATABASE_DATA: (dbId) => `${HOST}/api/v1/database/${dbId}`,
	CREATE_CLAIMABLE_DATABASE: (dbId, referrer) => `${HOST}/db?uuid=${dbId}${referrer ? `&ref=${referrer}` : ""}`,
	CLAIM_DATABASE: (dbId) => `${HOST}/database/${dbId}`,
	CREATE_DATABASE_POST: (dbId, referrer) => `${HOST}/api/v1/database/${dbId}${referrer ? `?referrer=${referrer}` : ""}`
};

//#endregion
export { LAUNCHPAD_URLS };
//# sourceMappingURL=urls.js.map